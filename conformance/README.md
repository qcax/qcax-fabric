# Conformance

Contains stable fixtures, vectors, semantic mutations, external-plugin fixtures and the runners that prove protocol/installed-image behavior. Generated results go only in `conformance/generated/` and do not modify source.
