# Repository-level development metadata only.
# Each publishable distribution has its own packages/*/pyproject.toml.
[dependency-groups]
test = [
  "jsonschema==4.26.0",
]
release = [
  "build",
  "packaging",
  "pypi-attestations",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
