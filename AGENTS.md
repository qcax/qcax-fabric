# Agent instructions

Read `spec/PROTOCOL.md`, `docs/ARCHITECTURE.md`, and `research/RESEARCH_SYSTEM.json` before modifying the core. Never make `TruthKernel` pluggable. Treat external framework docs and benchmarks as mechanism evidence only. Run `python scripts/run_all.py` after changes. Do not claim external conformance or production readiness from local tests.
