# Schemas

Versioned language-neutral QCAX Fabric schemas. Each schema must have positive fixtures, negative fixtures, canonicalization rules where relevant, and mutation coverage before it can be promoted.
